# QA E2E – Playwright

Automatizaciones E2E para la app de **PlayBásquet** usando [Playwright](https://playwright.dev/).

---

## 🚀 Requisitos

- **Node.js** 18 o superior  
- **npm** 8+ (o `yarn` / `pnpm`)  
- (Opcional) **VS Code** con extensión *Playwright Test for VS Code*

---

## ⚙️ Instalación

```bash
# Clonar el repositorio
git clone <URL-DEL-REPO>
cd <NOMBRE-DEL-REPO>

# Instalar dependencias
npm install

# Instalar navegadores requeridos por Playwright
npx playwright install
